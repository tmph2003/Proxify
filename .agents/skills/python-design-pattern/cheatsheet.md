## Python Design Patterns Cheatsheet

- Remember that each pattern has its own trade-offs.
- Python modules are already singletons, use them instead of writing custom Singleton classes.
- Favor composition over inheritance.
- Don't overuse design patterns in Python; simple functions often suffice.