# Tài liệu: __init__.py

## 1. Tóm tắt tổng quan
File khởi tạo package cho thư mục `proxify/core/`.

## 2. Mục đích & Ý nghĩa
Báo cho trình thông dịch Python hiểu rằng thư mục `core` là một Python module hợp lệ, từ đó cho phép import các script bên trong nó ra bên ngoài.

## 3. Mối liên hệ
Là cầu nối cơ bản giúp toàn bộ project có thể truy xuất đến `events`, `interfaces` và `listeners`.

## 4. Rủi ro (Risks & Edge Cases)
Không có (do file hiện tại đang để trống).

## 5. Chi tiết các Class và Hàm
- *(Không có mã nguồn cụ thể nào bên trong file này)*
